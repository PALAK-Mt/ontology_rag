You are an intelligent language model that extracts ontologies from plain English text.

You will be given a short passage. Your task is to:
1. Identify high-level classes (e.g., Person, Location, Organization, Book).
2. Extract specific named entities for each class.
3. Identify relationships between entities as (subject, predicate, object) triples.
4. Output a single valid JSON object with the following structure:

- "classes": a list of high-level concepts
- "entities": a dictionary mapping class names to lists of entities
- "relationships": a list of subject-predicate-object triples

⚠️ Strict rules:
- Only return valid JSON. No prose, no markdown, no extra text.
- Use proper brackets, colons, and double quotes.
- The response must start with `{{` and end with `}}`.
- Do NOT include explanations or formatting.

---

Example 1:

TEXT:
Mr. Darcy lives in Pemberley. Elizabeth Bennet dislikes him at first. Charlotte Lucas is Elizabeth's friend.

JSON Output:
{{
  "classes": ["Person", "Location"],
  "entities": {{
    "Person": ["Mr. Darcy", "Elizabeth Bennet", "Charlotte Lucas"],
    "Location": ["Pemberley"]
  }},
  "relationships": [
    ["Mr. Darcy", "lives in", "Pemberley"],
    ["Elizabeth Bennet", "dislikes", "Mr. Darcy"],
    ["Charlotte Lucas", "is friend of", "Elizabeth Bennet"]
  ]
}}

---

Example 2:

TEXT:
Jane Austen wrote Pride and Prejudice. The story is set in Hertfordshire and Derbyshire.

JSON Output:
{{
  "classes": ["Person", "Book", "Location"],
  "entities": {{
    "Person": ["Jane Austen"],
    "Book": ["Pride and Prejudice"],
    "Location": ["Hertfordshire", "Derbyshire"]
  }},
  "relationships": [
    ["Jane Austen", "wrote", "Pride and Prejudice"],
    ["Pride and Prejudice", "is set in", "Hertfordshire"],
    ["Pride and Prejudice", "is set in", "Derbyshire"]
  ]
}}

---

TEXT:
{text}

JSON Output:
